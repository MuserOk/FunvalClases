//functions
/*
const saludar = () => "Hola";
//console.log(saludar());

const saludarA = (nombre) => "Hola, " + nombre;
//console.log(saludoA("Mario"));

const calcCuadrado = numero => numero * numero;
//console.log(calcCuadrado(5));


const sumar = (a,b) => { 
    const resultado = a + b; 
    return resultado; 
}
//console.log(sumar(3,7));

const sumar = (a, b) => (a + b);
//console.log(sumar(3, 5));

 
const esPrimo = n => {
  if (n <= 1) return false;
  for (let i = 2; i <= Math.sqrt(n); i++) {
    if (n % i === 0) return false;
  }
  return true;
};
//console.log(esPrimo(7));   // true
//console.log(esPrimo(2));  // false


const fibonacci = n => {
  if (n <= 0) return 0;         // Caso base: F(0)
  if (n === 1) return 1;        // Caso base: F(1)
  return fibonacci(n - 1) + fibonacci(n - 2); // Recursión
};
//console.log(fibonacci(6));  // 8

const factorial = n => {
  if (n <= 1) return 1;
  return n * factorial(n - 1);
};
//console.log(factorial(5)); // 120

const esPar = n => n % 2 === 0;
const esImpar = n => !esPar(n);
//console.log(esPar(4));   // true
//console.log(esImpar(5)); // true


*/